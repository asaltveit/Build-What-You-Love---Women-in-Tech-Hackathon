# Convex (real-time grocery lists). Get your deployment URL from https://dashboard.convex.dev
# or run `npx convex dev` locally and copy the URL.
# For Replit: add this as a Secret (Tools → Secrets) so the production build gets the value.
VITE_CONVEX_URL=
